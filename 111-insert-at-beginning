public void insertAtStart(String cityName, List<String> cities) {
    //code here
    if(!String.isBlank(cityName) || cityName != ''){
        if(!cities.isEmpty() || cities != null ){
            cities.add(0, cityName);
        }else{
            cities.add(cityName);
        }
    }
}