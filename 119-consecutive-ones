public Integer maxConsecutiveOnes(Integer[] numbers) {
    //code here
    Integer count;
    if(!numbers.isEmpty()){
        for(Integer i = 0; i < numbers.size(); i++){
            if(numbers[i] == 1){
                count++;
            }else{
                count = 0;
            }
        }
    }
    
    return count;
}