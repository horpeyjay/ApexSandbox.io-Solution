public Integer nextPrime(Integer num) {    //code here
    Integer primeNumber = num + 1;
    while (true){
        if(isPrime(primeNumber)){
            return primeNumber;
        }else{
            primeNumber++;
        }
    }

    
}

public static Boolean isPrime(Integer num){
    if(num < 1){
        return false;
    }
    for(Integer i = 2; i < Math.sqrt(num); i++){
        if(num % i == 0){
            return false;
        }
    }
    return true;
}