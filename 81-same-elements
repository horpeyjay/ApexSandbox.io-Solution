public Boolean sameElements(List<Integer> nums1, List<Integer> nums2) {
    //code here
    if(!nums1.isEmpty() && !nums2.isEmpty()){
        Set<Integer> newList = new Set<Integer>();
        newList.add(nums1);
        if(newList.containsAll(nums2)){
            return true;
        }else{
            return false;
        }
    }
}