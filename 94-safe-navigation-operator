public String getAccountBillingCityWithSafeNavigation(List<Account> accounts){
   // Code here
   String final;
   String billingCity = accounts[0].BillingCity;
   if(billingCity != null){
       String result = accounts[0].BillingCity;
        final = result.toUpperCase();
   }else{
       final = 'null';
   }
}
